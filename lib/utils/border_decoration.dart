import 'package:flutter/material.dart';
import 'package:parkx/utils/app_theme.dart';

boxDecoration() {
  return BoxDecoration(
      color: Colors.white, borderRadius: const BorderRadius.all(Radius.circular(10)), border: Border.all(color: AppTheme.primaryColor));
}
